<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/burger_menu"
        android:icon="@drawable/icons8_menu_24"
        android:title=""
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/burger_menu_settings"
                android:icon="@android:drawable/ic_menu_preferences"
                android:title="@string/settings_activity_title"
                app:showAsAction="ifRoom">
            </item>

            <item
                android:id="@+id/burger_menu_about"
                android:icon="@android:drawable/ic_menu_info_details"
                android:title="@string/about_activity_title"
                app:showAsAction="ifRoom">
            </item>
        </menu>
    </item>
</menu>